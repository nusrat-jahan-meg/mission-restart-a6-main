live link:https://harmonious-toffee-1178da.netlify.app/

1) What is the difference between null and undefined?
Ans. Undefined:যখন কোনো variable value declare করা হয় না,
null: ইচ্ছা করে empty value সেট করা হয়
2) What is the use of the map() function in JavaScript? How is it different from forEach()?
Ans :map () Array এর প্রতিটা element নিয়ে কাজ করে new array return করে, ForEach() শুধু loop করার জন্য use হয়
3) What is the difference between == and ===?
Ans .== শুধু value check করে ,=== value and type check করে
4) What is the significance of async/await in fetching API data?
Ans. async/await allows us to handle asynchronous API calls in a clean and readable way. It makes the code look synchronous, avoids complex promise chaining, and provides better error handling using try/catch.
5) Explain the concept of Scope in JavaScript (Global, Function, Block).
Ans.Global Scope:function এর বাইরে declare করা হয়,সব জায়গা থেকে access করা যায়।
Function Scope:function এর বাইরে declare করলে বাইরে থেকে access করা যায় না|
Block Scope:let এবং const block scope follow করে,{ } এর ভিতরে declare করলে বাইরে থেকে access করা যায় না|